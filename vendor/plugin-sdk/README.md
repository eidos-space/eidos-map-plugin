# SDK type snapshot

Copied from `packages/plugin-sdk/src/index.ts` in the Eidos development workspace on 2026-09-16, including the unpublished table-view configuration and icon contracts. This snapshot is covered by the adjacent MIT license. It is a type-only build input, not a host runtime implementation or published SDK version. Replace it with a pinned SDK npm dependency when that package is released.
